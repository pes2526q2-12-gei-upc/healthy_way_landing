server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Vite build output lives under dist/landing/ (base: '/landing/').
    location /landing/ {
        try_files $uri $uri/ /landing/index.html;
    }

    location = /landing {
        return 301 /landing/;
    }
}
